import './Hero.scss';

export default function Hero(): JSX.Element {
  return (
    <section className="hero">
      <div className="hero__title-container">
        <h1 className="hero__title">happy holidays!</h1>
      </div>
    </section>
  );
}
